//console.log("working the core...");
function addFirst(a: any, b: number) {
    let x=4;
  if (typeof a === "number") console.log("type is " + typeof a);
  console.log(a + b);
}

add(3, 5);
//types typescript knows
//number, string,boolean,object,array |tuple, enums, any,union type, literal types

